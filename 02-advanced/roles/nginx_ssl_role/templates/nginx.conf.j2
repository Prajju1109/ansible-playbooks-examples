
server {
    listen 80;
    listen 443 ssl;
    server_name localhost;
    ssl_certificate /etc/ssl/certs/sample_cert.crt;
    ssl_certificate_key /etc/ssl/private/sample_key.key;
    location / {
        root /var/www/html;
        index index.html;
    }
}
